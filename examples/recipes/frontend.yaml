version: 1
name: "Frontend"
description: "macOS developer toolchain for frontend work (Node + core CLI)"

platform:
  os: [darwin]

ingredient_sources:
  - path: ../ingredients

ingredients:
  - base
  - node
